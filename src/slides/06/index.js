import React from 'react'
import { Box, Typography } from '@material-ui/core'

export default () => {
  return (
    <Box>
      <Typography variant="h2">Разве может что-то пойти не так?</Typography>
      <Typography variant="h2">Может.</Typography>
    </Box>
  )
}
